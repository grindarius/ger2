import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return <div>Showing your own information and personal data, differs based on your role. Requires custom sidebar layout for this page.</div>
})
