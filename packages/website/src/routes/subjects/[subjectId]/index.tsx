import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return (
    <div>
      Show information about a subject based on given semester id and stuffs through query params
    </div>
  )
})
