import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return (
    <div>
      a page to view student scores and give them grades. support importing grades from csv file.
    </div>
  )
})
