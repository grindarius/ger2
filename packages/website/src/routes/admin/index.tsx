import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return (
    <div>
      Admin's landing page with charts showing incomes on first page. sidebar should be showing
      Accounts management with submenu called, students, professors, admins. sidebar for subjects,
      calendar settings, grades, faculties, academic years. IAM.
    </div>
  )
})
