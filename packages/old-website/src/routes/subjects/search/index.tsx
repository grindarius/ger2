import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return <div>A page for searching an opening subjects for each semester. Have options for searching subjects and redirects to the subject information page.</div>
})
