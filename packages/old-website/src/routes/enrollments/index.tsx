import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return (
    <div>
      An enrollment page where it would allow students to select a subject and enroll to them.
    </div>
  )
})
